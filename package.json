{
  "name": "study-tour-lab-orchestration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "领域事件基础校验",
  "scripts": {
    "test": "node --test",
    "build": "node --check src/validator.js && node --check src/engine.js && node --check src/report.js && node --check src/checkpoint.js && node --check src/replay.js",
    "replay": "node src/replay.js data/event-stream.json",
    "replay:clean": "node src/replay.js data/event-stream.json --reset"
  }
}
